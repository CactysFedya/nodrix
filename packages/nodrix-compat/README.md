# Nodrix compatibility installer

The project is now named **Plyctl**. This metadata-only distribution keeps
`pip install nodrix` working by installing the matching `plyctl` release.

The `nodrix` Python package and command are shipped by Plyctl and remain
supported throughout the 2.x series.
