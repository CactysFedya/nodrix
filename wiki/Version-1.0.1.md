# Nodrix 1.0.1

Patch release for packaging and installation stability:

- Linux x86-64, Linux ARM64 and macOS Apple Silicon release wheels;
- Node.js 24 GitHub Actions;
- deterministic cibuildwheel 3.4.1;
- source builds with `setuptools>=68`;
- offline Raspberry Pi installation without the `packaging.licenses` failure;
- no API, ABI or manifest changes.
