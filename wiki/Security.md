# Security

Named streams support token authentication and IP/CIDR allowlists. Use `token_env`, not a literal token. Parser size limits are checked before allocation. Run artifacts redact secret-like fields. Remote package installation is not available in 1.0.
