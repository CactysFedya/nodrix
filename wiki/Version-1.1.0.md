# Nodrix 1.1.0

- Compact manifest and runtime profiles.
- Resolved configuration inspection and CLI overrides.
- Automatic media encoder probe.
- Per-node CPU/RAM telemetry and `nodrix top`.
- Publisher statistics in Nodrix Viewer.

See `docs/RELEASE_1.1.0.md` in the repository.
