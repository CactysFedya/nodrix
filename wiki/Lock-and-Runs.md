# Lock files and run artifacts

```bash
nodrix lock
nodrix run --locked
nodrix runs list
nodrix runs compare RUN_A RUN_B
```

The lock covers runtime, code, plugins, models, configs, schemas and installed package contents.
